# Agent 开发进度评估 (截至 2026-02-10)

## 核心状态概览

**"Foundation First. Speed Second."**
Agent 正处于从 "HTTP Worker" 向 "Native Scanner" 的关键转型期。
Cobra CLI 架构和 Core Task 模型已建立，Options 强类型参数解析已就绪。
**Phase 4 (编排与集成)** 已取得重大进展，**Phase 4.1 全流程编排** 已完成。
**Phase 4.2 (集群适配)** 已完成，**Agent 已具备完整的 Worker 能力 (拉取-执行-上报)**。
**Phase 4.3 (高级能力集成)** Web 模块已完成交付与集成，并完成了跨平台稳定性加固。
**Phase 5.1 (Web 爬虫与被动分析器)** 架构方案与实施文档已完成评审，**即将按 Sprint 0-5 顺序开工**，详见下方「下一步行动」与实施文档 [`docs/爬虫/Web爬虫与被动分析器实施文档-v1.0.md`](./爬虫/Web爬虫与被动分析器实施文档-v1.0.md)。

*   **Architecture (架构)**: **100%**。Cobra 命令树、Options 模式、Core Task 模型已完成，Reporter 接口已定义。
*   **Core Capability (核心能力)**: **100%**。
    *   IpAliveScanner: 🟢 (RTT/TTL/OS/Hostname/CIDR支持)
    *   PortScanner: 🟢 (TCP Connect + CSV/JSON Export)
    *   ServiceScanner: 🟢 (Nmap Engine + Custom Rules + **Dynamic Scheduling**)
    *   OSScanner: 🟢 (TCP/IP Stack Fingerprint + Service Banner + Unified Output)
    *   **WebScanner**: 🟢 (Headless Browser + Fingerprint + Screenshot + JS Vars + **Linux Support**)
*   **Orchestration (编排)**: **100%**。
    *   Pipeline: 🟢 (Alive -> Port -> Service -> OS -> Web 自动化串联)
    *   TargetGenerator: 🟢 (支持 CIDR/Range/List 解析)
    *   AutoRunner: 🟢 (支持并发扫描、结果汇总、Funnel 过滤)
*   **Infrastructure (基础设施)**: **95%**。Nmap 规则库静态打包完成，网络库 (Dialer/Proxy) 重构完成，Raw Socket 支持就绪。
*   **Connectivity (连接性)**: **100%**。Master-Agent 通信闭环已打通，支持 Token 认证、心跳、任务拉取与结果上报。

---

## 1. 模块详细评估

### 1.1 Core Decoupling (核心解耦) - "The Foundation"
| 组件 | 状态 | 备注 |
| :--- | :--- | :--- |
| **Directory Structure** | 🟢 **Done** | `internal/core` 已创建。 |
| **Task Model** | 🟢 **Done** | `model.Task` 已定义，支持多种扫描类型。 |
| **Service Logic** | 🟢 **Done** | Scanner/Runner 接口已定义 (`runner/interface.go`)。 |
| **Reporter** | 🟢 **Done** | `ConsoleReporter` (Tabular) 和 `CsvReporter` 已实现并统一。 |

### 1.2 CLI Transformation (CLI 改造) - "The Interface"
| 组件 | 状态 | 备注 |
| :--- | :--- | :--- |
| **Cobra Entry** | 🟢 **Done** | `main.go` 已重写，`root.go` 集成 Viper。 |
| **Server Command** | 🟢 **Done** | 支持 `--master` 和 `--token` 参数。 |
| **Scan Command** | 🟢 **Done** | 8种扫描子命令 (Alive, Port, Service, OS, Web, Dir, Vuln, Subdomain) 已就绪。 |
| **Proxy Command** | 🟢 **Done** | 支持 Socks5/HTTP/PortForward 模式参数解析。 |
| **Output Config** | 🟢 **Done** | 全局支持 CSV/JSON 输出 (`--oc`, `--oj`)，且 OS/Port/Web 扫描已适配。 |

### 1.3 Native Capabilities (原生能力) - "The Power"
| 组件 | 状态 | 备注 |
| :--- | :--- | :--- |
| **Concurrency** | 🟢 **Done** | 基于 Semaphore 的并发控制，支持 WaitGroup 调度。 |
| **Host Discovery** | 🟢 **Done** | `IpAliveScanner` 已实现 ARP/ICMP/TCP 探测，支持 OS/Hostname/RTT/TTL 解析。 |
| **Port Scan** | 🟢 **Done** | 原生 TCP Connect Scan，支持并发控制，CLI 输出已统一。 |
| **Service Scan** | 🟢 **Done** | `PortServiceScanner` 整合 Gonmap 引擎，支持 Nmap 探针和指纹匹配。**新增智能调度**。 |
| **OS Finger** | 🟢 **Done** | 实现了 Nmap TCP/IP 栈指纹识别 (Loopback 优化) + 服务 Banner 辅助识别。 |
| **Web Finger** | 🟢 **Done** | 基于 go-rod 的全栈扫描，支持 Screenshot/TechStack/Headers 提取与匹配。**已加固 Linux 稳定性**。 |

### 1.4 Cluster Integration (集群集成) - "The Connection"
| 组件 | 状态 | 备注 |
| :--- | :--- | :--- |
| **Join Protocol** | 🟢 **Done** | CLI 参数已支持 Token，废除独立的 Join 命令。 |
| **Auth Storage** | 🟢 **Done** | Viper 绑定 Token，内存/文件配置支持。 |
| **Task Pulling** | 🟢 **Done** | `internal/service/task` 实现了 Worker Loop，支持长轮询。 |

### 1.5 Scan Orchestration (全流程编排) - "The Brain"
| 组件 | 状态 | 备注 |
| :--- | :--- | :--- |
| **Pipeline Core** | 🟢 **Done** | `PipelineRunner` 已实现，支持 Stage 间数据流转。 |
| **Target Gen** | 🟢 **Done** | 支持 CIDR (192.168.1.0/24), Range, List 解析。 |
| **Auto Command** | 🟢 **Done** | `scan run` 命令已就绪，支持 `--summary` 汇总报告。 |
| **Funnel Filter** | 🟢 **Done** | 实现了 Alive -> Port -> Service -> Web 的漏斗式过滤。 |

---

## 2. 下一步行动 (Next Steps)

**"Stop designing. Start coding."**

### P0: Phase 5.1 - Web 扫描深度增强 (Web Crawler & Passive Analyzer)
*   **目标**: 增强 Web 扫描器的侦察能力，从"单点首页探测"升级为"深度爬取与被动信息收集"，为漏洞扫描提供充足的输入源。同时实现"是否爬取"的自动决策，减少人工参数干预。
*   **状态**: 🏃 **进行中**（架构方案 + 实施文档已定稿，代码开发未开始，**当前进度：Sprint 0 待启动**）
*   **依据文档**:
    *   架构方案：`docs/爬虫/Web爬虫与被动分析器架构方案-sonnet5-v3.0.md`（回答"为什么这么设计"）
    *   施工图纸：`docs/爬虫/Web爬虫与被动分析器实施文档-v1.0.md`（回答"改哪个文件、怎么验收"，**后续每完成一个 Sprint 在此处勾选**）
*   **行动**（严格按 Sprint 顺序开发，不允许跳跃，每个 Sprint 验收通过才能进入下一个）:
    1.  - [ ] **Sprint 0** - 依赖与目录骨架：引入 `goquery`，创建 `internal/core/scanner/web/crawler/` 包骨架，`WebResult` 新增 `Depth/Forms/Params/Leaks` 字段。
    2.  - [ ] **Sprint 1** - `crawler` 核心 BFS：并发 worker 池、URL 去重归一化、深度/页数上限、`EnqueueExtra` 预留接口，`-race` 并发正确性测试通过。
    3.  - [ ] **Sprint 2** - 攻击面提取 `extract.go`：基于 `goquery` 提取链接/表单/URL 参数，接入 go-rod 页面的 `ExtractLinks`。
    4.  - [ ] **Sprint 3** - 被动泄露检测 `leak.go`：AK/SK、JWT、内网 IP 等正则规则 + 强制脱敏 (`mask`)。
    5.  - [ ] **Sprint 4** - `web_scanner.go` 收口重构：`fallbackScan` → `fallbackFetch`，新增 `buildWebResult` 统一收口（go-rod/fallback/爬虫子页面三条数据源复用同一收口逻辑），同步修复现有单测，功能零回归。
    6.  - [ ] **Sprint 5** - 集成与联调：
        - [ ] 5.1 `Run()` 主干改造，挂上 crawler，统一降级路径（修复"浏览器不可用时爬虫失效"缺陷）。
        - [ ] 5.2 `decideCrawlDepth`/`resolveCrawlDepth` 自动决策（基于状态码/Content-Type/链接数三个免费信号，`auto`/`true`/`false` 三态）。
        - [ ] 5.3 三处接入点改造：`scan_web.go`（CLI 直接扫描）、`scan_run.go`+`dispatcher.go`（全流程编排）、`task_to_core.go`（Master 集群下发，删除硬编码 `crawl=true`）。
        - [ ] 5.4 按需浏览器升级 `escalateIfNeeded`：JS 重定向 / SPA 空壳三层静态检测，命中后对个位数页面做兜底渲染。
        - [ ] 5.5 端到端联调：按实施文档 7.5 节验证矩阵逐项跑通。
    7.  - [ ] **收尾**：全部 Sprint 验收通过后，更新本文件状态为已完成，并同步更新 `internal/core/scanner/web/README.md`。

### P1: Phase 5.2 - 漏洞扫描原子能力落地 (Vuln Scanner)
*   **目标**: 补齐真正的漏洞扫描引擎，实现基于技术栈的智能定向打击。
*   **状态**: ⏳ **待启动**
*   **行动**:
    1.  **实现 VulnScanner**: 在 `internal/core/scanner/vuln` 目录下封装 Nuclei 执行引擎。
    2.  **智能联动调度**: 修改 `dispatcher.go`，根据 WebScanner 识别出的 `TechStack` (技术栈) 动态过滤 Nuclei 模板，避免无效发包。
    3.  **打通主流程**: 替换 `dispatcher.go` 中的 `// TODO`，真实落地 "Vuln First" 调度策略，完善 `cmd/agent/scan/vuln.go`。

### P1: Phase 5.3 - 目录与子域名扫描 (Dir & Subdomain Scanner)
*   **目标**: 补齐主动试探维度的资产暴露面扫描能力。
*   **状态**: ⏳ **待启动**
*   **行动**:
    1.  **实现 DirScanner**: 在 `internal/core/scanner/dir` 下实现基于高并发 HTTP 请求的敏感路径/备份文件爆破。
    2.  **实现 SubdomainScanner**: 在 `internal/core/scanner/subdomain` 下实现基于并发 DNS 解析的枚举。
    3.  **CLI 联调**: 补全 `dir.go` 和 `subdomain.go` 的底层调用逻辑。

---

### 历史已完成阶段 (Archived)

### [已完成] Phase 4.1 - 全流程编排升级 (Parallel Pipeline)
*   **目标**: 将线性的 `PipelineRunner` 升级为支持 **并行分发** 和 **优先级控制** 的 v2.0 版本。
*   **状态**: 🟢 **已完成**
*   **行动**:
    1.  **分发器 (Dispatcher)**:
        - [x] 实现 `internal/core/pipeline/dispatcher.go`，定义分发规则 (Web/Vuln/Brute)。
    2.  **执行引擎 (Runner Refactor)**:
        - [x] 重构 `AutoRunner`，引入 `sync.WaitGroup` 管理并行 Worker。
        - [x] 实现 `Phase 1 (Sequential)`: Alive -> PortService。
        - [x] 实现 `Phase 2 (Parallel)`: 启动 WebWorker, VulnWorker, BruteWorker。
    3.  **依赖管理 (Dependency)**:
        - [x] 实现 `Vuln First, Brute Last` 策略，确保 Brute 任务在同端口 Vuln 任务完成后执行。

### [已完成] Phase 4.2 - 集群适配增强 (Cluster Adapter)
*   **目标**: 确保 Server 模式与 CLI 模式具备一致的扫描能力。
*   **状态**: 🟢 **已完成**
*   **行动**:
    1.  **统一初始化**: 重构 `setup/core.go`，移除手动实例化代码，复用 `RunnerManager` 的 Factory 逻辑。
    2.  **能力对齐**: Server 模式现已支持全功能爆破、智能超时控制和规则库加载。

### [已完成] Phase 4.3 - 高级能力集成 (Advanced Capabilities)
*   **目标**: 提升 Agent 的"重武器"能力。
*   **状态**: 🟢 **已完成**
*   **行动**:
    1.  集成 **Nuclei** 执行器 (作为独立 Scanner 或 Plugin)。
    2.  [x] 实现 **Web 指纹识别** (Wappalyzer/FingerprintHub 规则库适配)。
    3.  [x] 实现基础的 **Web 爬虫** (URL 采集)。
    4.  **Web 扫描器完善 (Web Scanner Polish)**:
        - [x] **JS Variables**: 实现动态提取 JS 全局变量以支持 NeoScan 内部指纹规则。
        - [x] **Integration**: 将 WebScanner 集成到 `ServiceDispatcher`，实现 `scan run` 自动化调用。
        - [x] **Verification**: 跑通 `scan run --web` 全流程测试，验证 CLI/CSV/JSON 输出一致性。
        - [x] **Stability**: 修复跨平台兼容性问题（Segfault, 权限丢失），增强 Panic Recovery 机制。

### [已完成] Phase 3.7 - 基础爆破 (Brute Force)
*   **目标**: 实现常见服务的弱口令检测，支持高并发且安全（防封禁）的爆破。
*   **状态**: 🟢 **已完成**
*   **行动**:
    1.  **基础设施 (Infrastructure)**:
        - [x] **Global Factory**: 实现了 `internal/core/factory`，统一管理 Brute, Alive, Port, OS 的实例化。
        - [x] **QoS**: 实现了基于反馈的自适应限流 (Adaptive Limiter)。
        - [x] **Test Cycle**: 解决了测试文件中的循环依赖问题。
        - [x] 定义 `Cracker` 接口 (借鉴 Qscan，纯粹验证逻辑)。
        - [x] 实现 `DictManager` (支持内置 Top100 + `%user%` 动态替换)。
        - [x] 实现 `BruteScanner` 调度器 (集成 `AdaptiveLimiter` 全局流控 + 单机串行)。
    2.  **协议实现 (Protocols)**:
        - [x] **系统服务**: SSH, RDP (Native Go), SMB, Telnet, FTP, SNMP
        - [x] **数据库**: MySQL, PostgreSQL, MSSQL, Oracle (SID探测+爆破), MongoDB, Redis, ClickHouse, Elasticsearch
    3.  **集成 (Integration)**:
        - [x] 注册到 RunnerManager。
        - [x] CLI 命令 `scan brute` 实现与验证 (支持多端口、全量模式、表格输出)。
        - [x] 端到端测试 (Covered: SSH, Mock Network Error, StopOnSuccess)。

---

## 3. 风险与阻碍 (Risks & Blockers)

*   **风险**: Web 模块 (Nuclei/Crawler) 可能会引入较大的二进制体积或内存开销。需要仔细权衡是 "Built-in" 还是 "Plugin/Sidecar"。
*   **阻碍**: 无。

---

## 4. 最近更新记录 (Changelog)

### [2026-07-30] Web 爬虫与被动分析器：架构设计与实施文档定稿
**核心交付**（本次为设计与文档产出，不含代码实现）:
1.  **自动决策机制设计**: 明确"是否触发深度爬取"由首页探测阶段的免费信号（状态码/Content-Type/链接数）自动判断，无需人工传参；同时保留 `auto`/`true`/`false` 三态显式开关，覆盖 CLI 单点扫描、`scan run` 全流程编排、Master 集群下发三条任务路径。
2.  **架构方案定稿**: `docs/爬虫/Web爬虫与被动分析器架构方案-sonnet5-v3.0.md`，明确"爬虫（主动）+ 被动分析器（伴生）"的双层定位、BFS 爬取、攻击面提取、被动泄露检测、JS 重定向/SPA 空壳的按需浏览器升级机制。
3.  **实施文档定稿**: `docs/爬虫/Web爬虫与被动分析器实施文档-v1.0.md`，拆分为 Sprint 0-5 共 6 个阶段，每阶段给出改动文件清单、函数签名、详细实现、单元测试要求、验收命令，并交叉核实了现状代码（行号、签名），修正了架构方案中的若干实现细节遗漏（如 `RichContext` 传递、队列关闭时机、`resolve` 边界处理等）。
4.  **下一步**: 按实施文档从 Sprint 0 开始逐阶段开发与验证，进度以本文件「P0: Phase 5.1」小节的 Sprint checklist 为准。

### [2026-02-10] Web Scanner 跨平台稳定性加固
**核心修复**:
1.  **Segfault Fix**: 通过 `CGO_ENABLED=0` 静态编译和添加 `Panic Recovery` 机制，彻底解决了 Linux 环境下因 ABI 不兼容导致的段错误。
2.  **Permission Fix**: 实现了 `BrowserManager` 的自动修复逻辑，在检测到 Chromium 二进制无执行权限时自动执行 `chmod +x`。
3.  **Status Code Accuracy**: 优化了 `NetworkResponseReceived` 事件监听逻辑，精准捕获 Document 类型的真实状态码，解决了 SPA 应用误报 404 的问题。

### [2026-02-10] Web Scanner 完善与集成
**核心交付**:
1.  **Integration Complete**: WebScanner 已完全集成至 `ServiceDispatcher`，`scan run` 流程可自动识别 Web 服务并触发 Web 扫描。
2.  **Output Consistency**: 统一了 CLI、JSON、CSV 的输出字段，确保 Title, TechStack, Server, Headers 等信息在所有输出格式中一致且完整。
3.  **Code Optimization**: 优化了 Dispatcher 的端口协议判断逻辑 (Switch Case Refactor)，提高了代码可读性。
4.  **Documentation**: 更新了 WebScanner 文档，修正了 Mermaid 语法错误，明确了内部指纹规则格式。

### [2026-02-06] Web Scanner 交付
**核心交付**:
1.  **Headless Browser 集成**: 引入 `go-rod`，实现了 `BrowserManager` (自动下载 Chromium) 和 `BrowserLauncher` (启动/代理配置)。
2.  **Web 扫描器**: 实现了 `WebScanner`，支持提取 DOM、Meta、Headers、Cookies、Favicon、Screenshot 等富上下文信息。
3.  **指纹识别**: 集成了 `Fingerprint` 引擎，支持加载 JSON 规则库并进行多维度匹配 (Body, Header, Title, Meta, Script)。
4.  **数据契约对齐**: `WebResult` 模型完全对齐 Master 端契约，支持 IP/Port/TechStack/Screenshot/Favicon 字段。
5.  **QoS & 线程安全**: 实现了基于事件监听的网络信息提取，并解决了并发访问的安全问题；集成了 `AdaptiveLimiter` 进行流控。

### [2026-02-05] Global Factory 重构 (Phase 1)
**架构优化**:
1.  **Scanner Factory**: 引入 `internal/core/factory` 包 (已迁移至顶层)，作为所有 Scanner 的统一实例化入口。
2.  **Brute Factory**: 实现了 `NewFullBruteScanner` 工厂方法，统一了 15+ 种爆破协议的注册逻辑。
3.  **DRY Refactor**: 重构了 `AutoRunner` 和 `RunnerManager`，消除了重复的 Cracker 注册代码，确保 CLI 单点扫描和全流程编排的能力一致性。
4.  **Verification**: 通过 `scan brute` 对 SSH 进行了回归测试，验证工厂模式工作正常。
