# NeoScan Master 环境变量配置示例
# 复制此文件为 .env 并放置在项目根目录下,根据实际情况修改配置值

# 注意: 生产环境中不建议将敏感信息直接写在 .env 文件中,
# 建议使用环境变量或安全的配置管理工具(如Vault)来存储敏感信息

# ===========================================
# 应用基础配置
# ===========================================

# 运行环境: development, test, production
NEOSCAN_APP_ENVIRONMENT=development

# 是否启用调试模式
NEOSCAN_APP_DEBUG=true

# 配置文件路径
NEOSCAN_CONFIG_PATH=configs

# ===========================================
# 服务器配置
# ===========================================

# 服务器监听地址
NEOSCAN_SERVER_HOST=0.0.0.0

# 服务器监听端口
NEOSCAN_SERVER_PORT=8080

# 服务器运行模式: debug, release, test
NEOSCAN_SERVER_MODE=debug

# ===========================================
# 数据库配置
# ===========================================

# MySQL 数据库配置
NEOSCAN_MYSQL_HOST=localhost
NEOSCAN_MYSQL_PORT=3306
NEOSCAN_MYSQL_USERNAME=root
NEOSCAN_MYSQL_PASSWORD=your_mysql_password
NEOSCAN_MYSQL_DATABASE=neoscan_dev

# Redis 数据库配置
NEOSCAN_REDIS_HOST=localhost
NEOSCAN_REDIS_PORT=6379
NEOSCAN_REDIS_PASSWORD=your_redis_password
NEOSCAN_REDIS_DATABASE=0

# ===========================================
# JWT 配置
# ===========================================

# JWT 密钥 (生产环境请使用强密码，至少32位)
NEOSCAN_JWT_SECRET=your_super_secret_jwt_key_at_least_32_characters_long

# ===========================================
# 安全配置
# ===========================================

# CORS 允许的源地址 (逗号分隔)
NEOSCAN_CORS_ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# CSRF 密钥
NEOSCAN_CSRF_SECRET=your_csrf_secret_key

# ===========================================
# 邮件配置
# ===========================================

# SMTP 服务器配置
NEOSCAN_MAIL_SMTP_HOST=smtp.gmail.com
NEOSCAN_MAIL_SMTP_PORT=587
NEOSCAN_MAIL_USERNAME=your_email@gmail.com
NEOSCAN_MAIL_PASSWORD=your_email_password
NEOSCAN_MAIL_FROM_EMAIL=noreply@neoscan.com

# ===========================================
# 第三方服务配置
# ===========================================

# 可以根据需要添加其他第三方服务的配置
# 例如：云存储、支付、短信等服务的API密钥

# ===========================================
# 开发环境特定配置
# ===========================================

# 开发环境可以添加一些调试相关的配置
# 例如：模拟数据、测试账号等

# ===========================================
# 注意事项
# ===========================================
# 1. 此文件包含敏感信息，请勿提交到版本控制系统
# 2. 生产环境请使用强密码和安全的配置
# 3. 定期更换密钥和密码
# 4. 使用环境变量管理敏感配置，避免硬编码
# 5. 不同环境使用不同的配置文件和环境变量