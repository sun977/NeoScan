# 高级规则编写指南

本文档将教您如何使用 `match` 字段编写高级指纹规则，适合需要复杂匹配逻辑的用户。

## 什么是高级规则？

高级规则是指使用 `match` 字段来定义复杂匹配逻辑的指纹规则。这种方式支持：
- 所有匹配操作符（equals, contains, regex, in, greater_than 等）
- 复杂的逻辑组合（AND/OR 嵌套）
- 自定义字段匹配
- 高级功能（数值比较、存在性检查等）

## match 字段的基本格式

`match` 字段支持两种格式：

### 格式1：正则表达式

```json
{
  "name": "WordPress",
  "match": "wp-.*"
}
```

**说明**：使用正则表达式匹配 `all_response` 字段。

**适用场景**：
- 需要使用正则表达式
- 简单的正则匹配

### 格式2：JSON 规则

```json
{
  "name": "WordPress",
  "match": {
    "field": "title",
    "operator": "contains",
    "value": "Log In"
  }
}
```

**说明**：使用 JSON 格式定义匹配规则。

**适用场景**：
- 需要使用特定操作符
- 需要组合多个条件
- 需要使用高级功能

## JSON 规则的基本结构

一个 JSON 规则包含以下字段：

```json
{
  "field": "字段名",
  "operator": "操作符",
  "value": "值",
  "ignore_case": true
}
```

### 字段说明

| 字段名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| `field` | string | 是 | 要匹配的字段名 | "title" |
| `operator` | string | 是 | 操作符 | "contains" |
| `value` | any | 是 | 要匹配的值 | "Log In" |
| `ignore_case` | boolean | 否 | 是否忽略大小写 | `true` |

## 字段名（field）

`field` 指定要匹配的字段，可以使用以下字段：

| 字段名 | 说明 | 示例 |
|-------|------|------|
| `status_code` | HTTP 状态码 | "200" |
| `title` | 网页标题 | "WordPress › Log In" |
| `body` | 响应体内容 | "<!DOCTYPE html>..." |
| `headers` | 所有响应头 | {"Server": "nginx"} |
| `all_headers` | 所有响应头的字符串形式 | "Server: nginx\n..." |
| `server` | Server 响应头 | "nginx/1.18.0" |
| `x_powered_by` | X-Powered-By 响应头 | "PHP/7.4.3" |
| `all_response` | 所有响应的字符串形式 | "Server: nginx\n<!DOCTYPE html>..." |

## 操作符（operator）

### 字符串操作符

#### equals - 精确匹配

```json
{
  "field": "status_code",
  "operator": "equals",
  "value": "200"
}
```

**说明**：`field` 的值必须完全等于 `value`。

**示例**：
```json
{
  "name": "WordPress",
  "match": {
    "field": "status_code",
    "operator": "equals",
    "value": "200"
  }
}
```

#### not_equals - 精确不匹配

```json
{
  "field": "status_code",
  "operator": "not_equals",
  "value": "404"
}
```

**说明**：`field` 的值不能等于 `value`。

**示例**：
```json
{
  "name": "WordPress",
  "match": {
    "field": "status_code",
    "operator": "not_equals",
    "value": "404"
  }
}
```

#### contains - 包含匹配

```json
{
  "field": "title",
  "operator": "contains",
  "value": "Log In",
  "ignore_case": true
}
```

**说明**：`field` 的值必须包含 `value`。

**示例**：
```json
{
  "name": "WordPress",
  "match": {
    "field": "title",
    "operator": "contains",
    "value": "Log In",
    "ignore_case": true
  }
}
```

#### not_contains - 不包含匹配

```json
{
  "field": "title",
  "operator": "not_contains",
  "value": "Error",
  "ignore_case": true
}
```

**说明**：`field` 的值不能包含 `value`。

**示例**：
```json
{
  "name": "WordPress",
  "match": {
    "field": "title",
    "operator": "not_contains",
    "value": "Error",
    "ignore_case": true
  }
}
```

#### starts_with - 以...开头

```json
{
  "field": "title",
  "operator": "starts_with",
  "value": "WordPress"
}
```

**说明**：`field` 的值必须以 `value` 开头。

**示例**：
```json
{
  "name": "WordPress",
  "match": {
    "field": "title",
    "operator": "starts_with",
    "value": "WordPress"
  }
}
```

#### ends_with - 以...结尾

```json
{
  "field": "server",
  "operator": "ends_with",
  "value": ".0"
}
```

**说明**：`field` 的值必须以 `value` 结尾。

**示例**：
```json
{
  "name": "Nginx",
  "match": {
    "field": "server",
    "operator": "ends_with",
    "value": ".0"
  }
}
```

#### regex - 正则表达式匹配

```json
{
  "field": "all_response",
  "operator": "regex",
  "value": "wp-.*"
}
```

**说明**：`field` 的值必须匹配正则表达式 `value`。

**示例**：
```json
{
  "name": "WordPress",
  "match": {
    "field": "all_response",
    "operator": "regex",
    "value": "wp-.*"
  }
}
```

**正则表达式示例**：
```json
{
  "field": "all_response",
  "operator": "regex",
  "value": "WordPress\\s*\\d+\\.\\d+\\.\\d+"
}
```

#### like - SQL LIKE 风格匹配

```json
{
  "field": "title",
  "operator": "like",
  "value": "%Log In%"
}
```

**说明**：`field` 的值必须匹配 LIKE 模式，支持 `%` 和 `_` 通配符。

**示例**：
```json
{
  "name": "WordPress",
  "match": {
    "field": "title",
    "operator": "like",
    "value": "%Log In%"
  }
}
```

### 数值操作符

#### greater_than - 大于

```json
{
  "field": "content_length",
  "operator": "greater_than",
  "value": 1000
}
```

**说明**：`field` 的值必须大于 `value`。

**示例**：
```json
{
  "name": "Large Page",
  "match": {
    "field": "content_length",
    "operator": "greater_than",
    "value": 10000
  }
}
```

#### less_than - 小于

```json
{
  "field": "content_length",
  "operator": "less_than",
  "value": 10000
}
```

**说明**：`field` 的值必须小于 `value`。

**示例**：
```json
{
  "name": "Small Page",
  "match": {
    "field": "content_length",
    "operator": "less_than",
    "value": 1000
  }
}
```

#### greater_than_or_equal - 大于等于

```json
{
  "field": "content_length",
  "operator": "greater_than_or_equal",
  "value": 1000
}
```

**说明**：`field` 的值必须大于或等于 `value`。

#### less_than_or_equal - 小于等于

```json
{
  "field": "content_length",
  "operator": "less_than_or_equal",
  "value": 10000
}
```

**说明**：`field` 的值必须小于或等于 `value`。

### 集合操作符

#### in - 在列表中

```json
{
  "field": "status_code",
  "operator": "in",
  "value": ["200", "301", "302"]
}
```

**说明**：`field` 的值必须在 `value` 列表中。

**示例**：
```json
{
  "name": "WordPress",
  "match": {
    "field": "status_code",
    "operator": "in",
    "value": ["200", "301", "302"]
  }
}
```

#### not_in - 不在列表中

```json
{
  "field": "status_code",
  "operator": "not_in",
  "value": ["404", "500"]
}
```

**说明**：`field` 的值不能在 `value` 列表中。

**示例**：
```json
{
  "name": "WordPress",
  "match": {
    "field": "status_code",
    "operator": "not_in",
    "value": ["404", "500"]
  }
}
```

#### list_contains - 列表包含某个值

```json
{
  "field": "headers",
  "operator": "list_contains",
  "value": "nginx"
}
```

**说明**：`field` 的值（列表）必须包含 `value`。

**示例**：
```json
{
  "name": "Nginx",
  "match": {
    "field": "headers",
    "operator": "list_contains",
    "value": "nginx"
  }
}
```

### 存在性操作符

#### exists - 字段存在

```json
{
  "field": "server",
  "operator": "exists"
}
```

**说明**：`field` 字段必须存在。

**示例**：
```json
{
  "name": "Has Server Header",
  "match": {
    "field": "server",
    "operator": "exists"
  }
}
```

#### is_null - 字段为空

```json
{
  "field": "x_powered_by",
  "operator": "is_null"
}
```

**说明**：`field` 字段必须为空。

**示例**：
```json
{
  "name": "No X-Powered-By",
  "match": {
    "field": "x_powered_by",
    "operator": "is_null"
  }
}
```

#### is_not_null - 字段不为空

```json
{
  "field": "x_powered_by",
  "operator": "is_not_null"
}
```

**说明**：`field` 字段必须不为空。

**示例**：
```json
{
  "name": "Has X-Powered-By",
  "match": {
    "field": "x_powered_by",
    "operator": "is_not_null"
  }
}
```

### 网络操作符

#### cidr - IP 地址在 CIDR 范围内

```json
{
  "field": "ip",
  "operator": "cidr",
  "value": "192.168.1.0/24"
}
```

**说明**：`field` 的值（IP 地址）必须在 CIDR 范围内。

**示例**：
```json
{
  "name": "Internal IP",
  "match": {
    "field": "ip",
    "operator": "cidr",
    "value": "192.168.0.0/16"
  }
}
```

## 逻辑组合

### AND 逻辑

所有条件都必须满足。

```json
{
  "and": [
    {"field": "status_code", "operator": "equals", "value": "200"},
    {"field": "title", "operator": "contains", "value": "Log In"}
  ]
}
```

**示例**：
```json
{
  "name": "WordPress",
  "match": {
    "and": [
      {"field": "status_code", "operator": "equals", "value": "200"},
      {"field": "title", "operator": "contains", "value": "Log In"},
      {"field": "body", "operator": "contains", "value": "wp-content"}
    ]
  }
}
```

**说明**：只有当以下所有条件都满足时，才匹配成功：
- HTTP 状态码为 200
- 网页标题包含 "Log In"
- 响应体包含 "wp-content"

### OR 逻辑

只要有一个条件满足即可。

```json
{
  "or": [
    {"field": "status_code", "operator": "equals", "value": "200"},
    {"field": "status_code", "operator": "equals", "value": "301"}
  ]
}
```

**示例**：
```json
{
  "name": "WordPress",
  "match": {
    "or": [
      {"field": "status_code", "operator": "equals", "value": "200"},
      {"field": "status_code", "operator": "equals", "value": "301"},
      {"field": "status_code", "operator": "equals", "value": "302"}
    ]
  }
}
```

**说明**：只要 HTTP 状态码为 200、301 或 302 中的任意一个，就匹配成功。

### 嵌套逻辑

支持 AND 和 OR 的嵌套组合。

```json
{
  "and": [
    {"field": "title", "operator": "contains", "value": "Login"},
    {"field": "server", "operator": "regex", "value": "nginx.*"},
    {
      "or": [
        {"field": "status_code", "operator": "equals", "value": "200"},
        {"field": "status_code", "operator": "equals", "value": "301"}
      ]
    }
  ]
}
```

**示例**：
```json
{
  "name": "WordPress",
  "match": {
    "and": [
      {"field": "title", "operator": "contains", "value": "Login"},
      {"field": "server", "operator": "regex", "value": "nginx.*"},
      {
        "or": [
          {"field": "status_code", "operator": "equals", "value": "200"},
          {"field": "status_code", "operator": "equals", "value": "301"}
        ]
      }
    ]
  }
}
```

**说明**：只有当以下所有条件都满足时，才匹配成功：
- 网页标题包含 "Login"
- Server 响应头匹配正则表达式 "nginx.*"
- HTTP 状态码为 200 或 301

## 实战示例

### 示例1：使用正则表达式匹配版本号

```json
{
  "name": "WordPress",
  "match": {
    "field": "all_response",
    "operator": "regex",
    "value": "WordPress\\s+\\d+\\.\\d+\\.\\d+"
  }
}
```

**说明**：匹配包含 WordPress 版本号的响应。

### 示例2：组合多个条件（AND）

```json
{
  "name": "WordPress",
  "match": {
    "and": [
      {"field": "status_code", "operator": "equals", "value": "200"},
      {"field": "title", "operator": "contains", "value": "Log In"},
      {"field": "body", "operator": "contains", "value": "wp-content"}
    ]
  }
}
```

**说明**：匹配状态码为 200、标题包含 "Log In"、响应体包含 "wp-content" 的响应。

### 示例3：组合多个条件（OR）

```json
{
  "name": "WordPress",
  "match": {
    "or": [
      {"field": "title", "operator": "contains", "value": "WordPress"},
      {"field": "body", "operator": "contains", "value": "wp-content"},
      {"field": "body", "operator": "contains", "value": "wp-includes"}
    ]
  }
}
```

**说明**：只要标题包含 "WordPress" 或响应体包含 "wp-content" 或 "wp-includes"，就匹配成功。

### 示例4：嵌套逻辑

```json
{
  "name": "WordPress",
  "match": {
    "and": [
      {"field": "title", "operator": "contains", "value": "Login"},
      {
        "or": [
          {"field": "server", "operator": "contains", "value": "nginx"},
          {"field": "server", "operator": "contains", "value": "Apache"}
        ]
      }
    ]
  }
}
```

**说明**：标题包含 "Login" 且 Server 响应头包含 "nginx" 或 "Apache"，就匹配成功。

### 示例5：使用 in 操作符

```json
{
  "name": "WordPress",
  "match": {
    "and": [
      {"field": "title", "operator": "contains", "value": "Log In"},
      {"field": "status_code", "operator": "in", "value": ["200", "301", "302"]}
    ]
  }
}
```

**说明**：标题包含 "Log In" 且状态码为 200、301 或 302，就匹配成功。

### 示例6：使用 exists 操作符

```json
{
  "name": "Has X-Powered-By",
  "match": {
    "field": "x_powered_by",
    "operator": "exists"
  }
}
```

**说明**：只要存在 X-Powered-By 响应头，就匹配成功。

### 示例7：使用 is_null 操作符

```json
{
  "name": "No X-Powered-By",
  "match": {
    "and": [
      {"field": "title", "operator": "contains", "value": "Login"},
      {"field": "x_powered_by", "operator": "is_null"}
    ]
  }
}
```

**说明**：标题包含 "Login" 且不存在 X-Powered-By 响应头，就匹配成功。

### 示例8：复杂嵌套逻辑

```json
{
  "name": "WordPress",
  "match": {
    "and": [
      {"field": "title", "operator": "contains", "value": "Login"},
      {
        "or": [
          {"field": "server", "operator": "contains", "value": "nginx"},
          {"field": "server", "operator": "contains", "value": "Apache"},
          {"field": "server", "operator": "contains", "value": "IIS"}
        ]
      },
      {
        "or": [
          {"field": "body", "operator": "contains", "value": "wp-content"},
          {"field": "body", "operator": "contains", "value": "wp-includes"},
          {"field": "body", "operator": "regex", "value": "wp-.*"}
        ]
      }
    ]
  }
}
```

**说明**：
- 标题包含 "Login"
- 且 Server 响应头包含 "nginx" 或 "Apache" 或 "IIS"
- 且响应体包含 "wp-content" 或 "wp-includes" 或匹配正则 "wp-.*"

## 混合使用简单字段和 match 字段

您可以在一个规则中同时使用简单字段和 match 字段。

### 示例

```json
{
  "name": "WordPress",
  "status_code": "200",
  "title": "Log In",
  "match": "wp-.*"
}
```

**说明**：
- `status_code` 和 `title` 是简单字段
- `match` 是正则表达式
- 所有条件都必须满足（AND 逻辑）

### 编译后的规则

```json
{
  "and": [
    {"field": "status_code", "operator": "equals", "value": "200"},
    {"field": "title", "operator": "contains", "value": "Log In", "ignore_case": true},
    {"field": "all_response", "operator": "regex", "value": "wp-.*"}
  ]
}
```

## 常见问题

### Q: 正则表达式和 JSON 规则有什么区别？

A:
- **正则表达式**：简单快捷，适合简单的正则匹配
- **JSON 规则**：功能强大，支持所有操作符和逻辑组合

### Q: 如何选择使用哪种格式？

A:
- 简单的正则匹配 → 使用正则表达式
- 需要特定操作符 → 使用 JSON 规则
- 需要组合多个条件 → 使用 JSON 规则
- 需要使用高级功能 → 使用 JSON 规则

### Q: 如何调试复杂的规则？

A:
1. 先测试每个单独的条件
2. 逐步组合条件
3. 查看日志了解匹配过程
4. 使用测试工具验证正则表达式

### Q: ignore_case 参数有什么作用？

A: `ignore_case` 参数控制是否忽略大小写，默认为 `false`。

### Q: 可以自定义字段吗？

A: 可以，但需要在输入数据中存在该字段。目前系统自动提取的字段包括：`status_code`、`title`、`body`、`headers`、`all_headers`、`server`、`x_powered_by`、`all_response`。

## 最佳实践

1. **选择合适的格式**：简单场景使用正则表达式，复杂场景使用 JSON 规则
2. **组合多个条件**：组合多个条件可以提高准确性
3. **使用嵌套逻辑**：合理使用 AND/OR 嵌套，避免过于复杂的规则
4. **测试规则**：编写规则后，务必进行测试
5. **查看日志**：查看日志了解匹配过程，帮助调试
6. **优化性能**：避免过于复杂的正则表达式，影响匹配性能

## 下一步

- 📚 [规则字段详解](./04_规则字段详解.md) - 查看所有可用字段的详细说明
- 💡 [常见示例](./05_常见示例.md) - 查看更多常见应用的指纹规则示例
- ✨ [最佳实践](./06_最佳实践.md) - 学习编写高质量规则的最佳实践
