# 规则字段详解

本文档详细介绍了 NeoScan 指纹识别系统中所有可用的规则字段。

## 规则字段概览

| 字段名 | 类型 | 必填 | 说明 | 匹配字段 | 操作符 | 大小写敏感 |
|-------|------|------|------|---------|--------|----------|
| `name` | string | 是 | 指纹名称 | - | - | - |
| `status_code` | string | 否 | HTTP 状态码 | `status_code` | `equals` | 是 |
| `url` | string | 否 | URL 路径 | - | - | - |
| `title` | string | 否 | 网页标题 | `title` | `contains` | 否 |
| `subtitle` | string | 否 | 网页副标题 | `body` | `contains` | 否 |
| `footer` | string | 否 | 网页页脚 | `body` | `contains` | 否 |
| `header` | string | 否 | HTTP 响应头 | `all_headers` | `contains` | 否 |
| `response` | string | 否 | HTTP 响应内容 | `body` | `contains` | 否 |
| `server` | string | 否 | Server 响应头 | `server` 或 `all_headers` | `contains` | 否 |
| `x_powered_by` | string | 否 | X-Powered-By 响应头 | `x_powered_by` 或 `all_headers` | `contains` | 否 |
| `body` | string | 否 | HTTP 响应体 | `body` | `contains` | 否 |
| `match` | string | 否 | 高级匹配规则 | `all_response` | `regex` 或 JSON | 取决于规则 |
| `enabled` | boolean | 否 | 是否启用 | - | - | - |
| `source` | string | 否 | 来源标识 | - | - | - |

## 必填字段

### name - 指纹名称

**类型**：string  
**必填**：是  
**说明**：指纹的名称，用于标识识别出的应用或服务。

**示例**：
```json
{
  "name": "WordPress"
}
```

**注意事项**：
- 名称应该简洁明了
- 建议使用应用的官方名称
- 避免使用特殊字符

## 可选字段

### status_code - HTTP 状态码

**类型**：string  
**必填**：否  
**说明**：匹配 HTTP 响应的状态码。

**匹配字段**：`status_code`  
**操作符**：`equals`  
**大小写敏感**：是

**示例**：
```json
{
  "name": "WordPress",
  "status_code": "200"
}
```

**常见状态码**：
- `200` - 成功
- `301` - 永久重定向
- `302` - 临时重定向
- `404` - 未找到
- `500` - 服务器错误

**注意事项**：
- 状态码是字符串类型，需要加引号
- 使用精确匹配（equals）

### url - URL 路径

**类型**：string  
**必填**：否  
**说明**：指定访问的 URL 路径。

**匹配字段**：-（不参与匹配）  
**操作符**：-  
**大小写敏感**：-

**示例**：
```json
{
  "name": "WordPress",
  "url": "/wp-login.php"
}
```

**注意事项**：
- `url` 字段不参与匹配，仅用于文档或扫描时指定访问路径
- 扫描器会访问指定的 URL 路径

### title - 网页标题

**类型**：string  
**必填**：否  
**说明**：匹配网页的标题（从 HTML 的 `<title>` 标签提取）。

**匹配字段**：`title`  
**操作符**：`contains`  
**大小写敏感**：否

**示例**：
```json
{
  "name": "WordPress",
  "title": "Log In"
}
```

**注意事项**：
- 标题是从 HTML 中自动提取的
- 使用包含匹配（contains）
- 默认忽略大小写

### subtitle - 网页副标题

**类型**：string  
**必填**：否  
**说明**：匹配网页副标题的内容。

**匹配字段**：`body`  
**操作符**：`contains`  
**大小写敏感**：否

**示例**：
```json
{
  "name": "WordPress",
  "subtitle": "WordPress"
}
```

**注意事项**：
- `subtitle` 是 `body` 的别名
- 使用包含匹配（contains）
- 默认忽略大小写

### footer - 网页页脚

**类型**：string  
**必填**：否  
**说明**：匹配网页页脚的内容。

**匹配字段**：`body`  
**操作符**：`contains`  
**大小写敏感**：否

**示例**：
```json
{
  "name": "WordPress",
  "footer": "Powered by WordPress"
}
```

**注意事项**：
- `footer` 是 `body` 的别名
- 使用包含匹配（contains）
- 默认忽略大小写
- 通常用于匹配页脚中的版权信息

### header - HTTP 响应头

**类型**：string  
**必填**：否  
**说明**：匹配任意 HTTP 响应头的内容。

**匹配字段**：`all_headers`  
**操作符**：`contains`  
**大小写敏感**：否

**示例**：
```json
{
  "name": "Nginx",
  "header": "nginx"
}
```

**注意事项**：
- 会检查所有响应头
- 使用包含匹配（contains）
- 默认忽略大小写

### response - HTTP 响应内容

**类型**：string  
**必填**：否  
**说明**：匹配 HTTP 响应体的内容。

**匹配字段**：`body`  
**操作符**：`contains`  
**大小写敏感**：否

**示例**：
```json
{
  "name": "WordPress",
  "response": "wp-content"
}
```

**注意事项**：
- `response` 是 `body` 的别名
- 使用包含匹配（contains）
- 默认忽略大小写

### server - Server 响应头

**类型**：string  
**必填**：否  
**说明**：匹配 `Server` 响应头的内容。

**匹配字段**：`server` 或 `all_headers`（OR 逻辑）  
**操作符**：`contains`  
**大小写敏感**：否

**示例**：
```json
{
  "name": "Nginx",
  "server": "nginx"
}
```

**注意事项**：
- 使用 OR 逻辑匹配（`server` 或 `all_headers`）
- 使用包含匹配（contains）
- 默认忽略大小写

### x_powered_by - X-Powered-By 响应头

**类型**：string  
**必填**：否  
**说明**：匹配 `X-Powered-By` 响应头的内容。

**匹配字段**：`x_powered_by` 或 `all_headers`（OR 逻辑）  
**操作符**：`contains`  
**大小写敏感**：否

**示例**：
```json
{
  "name": "PHP",
  "x_powered_by": "PHP"
}
```

**注意事项**：
- 使用 OR 逻辑匹配（`x_powered_by` 或 `all_headers`）
- 使用包含匹配（contains）
- 默认忽略大小写

### body - HTTP 响应体

**类型**：string  
**必填**：否  
**说明**：匹配 HTTP 响应体的内容。

**匹配字段**：`body`  
**操作符**：`contains`  
**大小写敏感**：否

**示例**：
```json
{
  "name": "WordPress",
  "body": "wp-content"
}
```

**注意事项**：
- 使用包含匹配（contains）
- 默认忽略大小写
- 会检查整个响应体

### match - 高级匹配规则

**类型**：string  
**必填**：否  
**说明**：使用高级匹配规则，支持正则表达式和 JSON 格式。

**匹配字段**：`all_response`  
**操作符**：`regex` 或 JSON  
**大小写敏感**：取决于规则

#### 格式1：正则表达式

```json
{
  "name": "WordPress",
  "match": "wp-.*"
}
```

**说明**：使用正则表达式匹配 `all_response` 字段。

#### 格式2：JSON 规则

```json
{
  "name": "WordPress",
  "match": {
    "field": "title",
    "operator": "contains",
    "value": "Log In"
  }
}
```

**说明**：使用 JSON 格式定义匹配规则。

**注意事项**：
- 正则表达式格式：直接写正则表达式
- JSON 规则格式：以 `{` 开头的 JSON 字符串
- 支持 AND/OR 嵌套逻辑
- 支持所有操作符

### enabled - 是否启用

**类型**：boolean  
**必填**：否  
**默认值**：`true`  
**说明**：控制规则是否启用。

**示例**：
```json
{
  "name": "WordPress",
  "enabled": true
}
```

**注意事项**：
- `true` - 启用规则
- `false` - 禁用规则
- 禁用的规则不会参与匹配

### source - 来源标识

**类型**：string  
**必填**：否  
**默认值**：`"system"`  
**说明**：标识规则的来源。

**示例**：
```json
{
  "name": "WordPress",
  "source": "custom"
}
```

**常见值**：
- `"system"` - 系统内置规则
- `"custom"` - 用户自定义规则

**注意事项**：
- 用于区分规则的来源
- 可以用于规则管理

## 匹配字段详解

系统会将 HTTP 响应转换为以下匹配字段：

### status_code

**说明**：HTTP 状态码  
**来源**：响应行  
**示例**：`"200"`

### title

**说明**：网页标题  
**来源**：从 HTML 的 `<title>` 标签提取  
**示例**：`"WordPress › Log In"`

### body

**说明**：HTTP 响应体  
**来源**：HTTP 响应体  
**示例**：`"<!DOCTYPE html>\n<html>\n..."`

### headers

**说明**：所有响应头  
**来源**：HTTP 响应头  
**示例**：`{"Server": "nginx", "Content-Type": "text/html"}`

### all_headers

**说明**：所有响应头的字符串形式  
**来源**：拼接所有响应头  
**示例**：`"Server: nginx\nContent-Type: text/html\n..."`

### server

**说明**：Server 响应头  
**来源**：`headers["Server"]`  
**示例**：`"nginx/1.18.0"`

### x_powered_by

**说明**：X-Powered-By 响应头  
**来源**：`headers["X-Powered-By"]`  
**示例**：`"PHP/7.4.3"`

### all_response

**说明**：所有响应的字符串形式  
**来源**：`all_headers + "\n" + body`  
**示例**：`"Server: nginx\n<!DOCTYPE html>\n<html>\n..."`

## 操作符详解

### 字符串操作符

| 操作符 | 说明 | 示例 |
|-------|------|------|
| `equals` | 精确匹配 | `{"field": "status_code", "operator": "equals", "value": "200"}` |
| `not_equals` | 精确不匹配 | `{"field": "status_code", "operator": "not_equals", "value": "404"}` |
| `contains` | 包含匹配 | `{"field": "title", "operator": "contains", "value": "Log In"}` |
| `not_contains` | 不包含匹配 | `{"field": "title", "operator": "not_contains", "value": "Error"}` |
| `starts_with` | 以...开头 | `{"field": "title", "operator": "starts_with", "value": "WordPress"}` |
| `ends_with` | 以...结尾 | `{"field": "server", "operator": "ends_with", "value": ".0"}` |
| `regex` | 正则表达式匹配 | `{"field": "all_response", "operator": "regex", "value": "wp-.*"}` |
| `like` | SQL LIKE 风格匹配 | `{"field": "title", "operator": "like", "value": "%Log In%"}` |

### 数值操作符

| 操作符 | 说明 | 示例 |
|-------|------|------|
| `greater_than` | 大于 | `{"field": "content_length", "operator": "greater_than", "value": 1000}` |
| `less_than` | 小于 | `{"field": "content_length", "operator": "less_than", "value": 10000}` |
| `greater_than_or_equal` | 大于等于 | `{"field": "content_length", "operator": "greater_than_or_equal", "value": 1000}` |
| `less_than_or_equal` | 小于等于 | `{"field": "content_length", "operator": "less_than_or_equal", "value": 10000}` |

### 集合操作符

| 操作符 | 说明 | 示例 |
|-------|------|------|
| `in` | 在列表中 | `{"field": "status_code", "operator": "in", "value": ["200", "301", "302"]}` |
| `not_in` | 不在列表中 | `{"field": "status_code", "operator": "not_in", "value": ["404", "500"]}` |
| `list_contains` | 列表包含某个值 | `{"field": "headers", "operator": "list_contains", "value": "nginx"}` |

### 存在性操作符

| 操作符 | 说明 | 示例 |
|-------|------|------|
| `exists` | 字段存在 | `{"field": "server", "operator": "exists"}` |
| `is_null` | 字段为空 | `{"field": "x_powered_by", "operator": "is_null"}` |
| `is_not_null` | 字段不为空 | `{"field": "x_powered_by", "operator": "is_not_null"}` |

### 网络操作符

| 操作符 | 说明 | 示例 |
|-------|------|------|
| `cidr` | IP 地址在 CIDR 范围内 | `{"field": "ip", "operator": "cidr", "value": "192.168.1.0/24"}` |

## 字段使用建议

### 选择合适的字段

1. **识别 Web 应用**：使用 `title`、`body`、`footer`
2. **识别 Web 服务器**：使用 `server`、`header`
3. **识别编程语言**：使用 `x_powered_by`
4. **识别框架**：使用 `header`、`body`
5. **识别状态**：使用 `status_code`

### 组合多个字段

组合多个字段可以提高准确性：

```json
{
  "name": "WordPress",
  "status_code": "200",
  "title": "Log In",
  "body": "wp-content"
}
```

### 使用 match 字段

当需要复杂匹配逻辑时，使用 `match` 字段：

```json
{
  "name": "WordPress",
  "match": {
    "and": [
      {"field": "status_code", "operator": "equals", "value": "200"},
      {"field": "title", "operator": "contains", "value": "Log In"}
    ]
  }
}
```

## 常见问题

### Q: url 字段为什么不参与匹配？

A: `url` 字段仅用于文档或扫描时指定访问路径，不参与匹配。扫描器会访问指定的 URL 路径，然后根据其他字段进行匹配。

### Q: 如何区分大小写？

A: 大多数简单字段（如 `title`、`header`、`body`）默认忽略大小写。如果需要区分大小写，请使用 `match` 字段并设置 `ignore_case: false`。

### Q: 可以自定义字段吗？

A: 可以，但需要在输入数据中存在该字段。目前系统自动提取的字段包括：`status_code`、`title`、`body`、`headers`、`all_headers`、`server`、`x_powered_by`、`all_response`。

### Q: 为什么有些字段使用 OR 逻辑？

A: `server` 和 `x_powered_by` 字段使用 OR 逻辑，因为它们既可以在特定的响应头中，也可以在 `all_headers` 中。这样可以提高匹配的灵活性。

## 下一步

- 💡 [常见示例](./05_常见示例.md) - 查看更多常见应用的指纹规则示例
- ✨ [最佳实践](./06_最佳实践.md) - 学习编写高质量规则的最佳实践
