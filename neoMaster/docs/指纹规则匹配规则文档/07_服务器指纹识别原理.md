# 服务器指纹识别原理详解

本文档将深入讲解 NeoScan 服务器指纹识别系统（ServiceEngine）的工作原理，帮助您理解服务器指纹规则是如何生效的。

## 什么是服务器指纹？

服务器指纹是指通过分析服务返回的 Banner 信息来识别服务器类型、版本、厂商等信息的技术。

### Banner 信息示例

当您连接到一个服务时，服务通常会返回 Banner 信息：

```
SSH-2.0-OpenSSH_8.2p1 Ubuntu-4ubuntu0.5
HTTP/1.1 200 OK
Server: nginx/1.18.0
220 ProFTPD 1.3.5 Server (Debian)
```

这些 Banner 信息包含了服务器的类型、版本等信息，服务器指纹就是通过分析这些信息来识别服务器。

## 系统架构概览

NeoScan 服务器指纹识别系统由以下几个核心组件组成：

```
┌─────────────────────────────────────────────────────────────┐
│  服务器指纹规则存储                                      │
│  - 数据库 (asset_cpe 表)                                │
│  - JSON 文件 (rules/fingerprint/)                         │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  规则加载器 (LoadRules)                                 │
│  - 读取数据库中的 CPE 规则                               │
│  - 读取 JSON 文件                                         │
│  - 编译正则表达式                                        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  匹配引擎 (ServiceEngine)                               │
│  - 加载编译后的规则                                      │
│  - 遍历规则进行匹配                                      │
│  - 提取版本号等信息                                       │
│  - 生成 CPE                                             │
│  - 返回匹配结果                                          │
└─────────────────────────────────────────────────────────────┘
```

## 服务器指纹识别的完整流程

### 1. 规则加载阶段

当系统启动时，会执行以下操作：

```
1. 读取数据库中的 CPE 规则
   ↓
2. 读取 rules/fingerprint/ 目录下的 JSON 文件
   ↓
3. 对每条规则编译正则表达式
   ↓
4. 将编译后的规则加载到内存
   ↓
5. 准备好进行服务器指纹识别
```

### 2. 服务器指纹识别阶段

当扫描器扫描到一个服务时，会执行以下操作：

```
1. 连接到服务，获取 Banner 信息
   ↓
2. 检查是否有 Banner 信息
   ↓
3. 遍历所有编译后的规则
   ↓
4. 对每条规则使用正则表达式匹配 Banner
   ↓
5. 如果匹配成功，提取版本号等信息
   ↓
6. 填充 CPE（Common Platform Enumeration）
   ↓
7. 生成匹配结果
   ↓
8. 返回所有匹配的服务器指纹
```

## 规则格式详解

### CPE 规则结构

```json
{
  "match_str": "(?i)nginx/([\\d\\.]+)",
  "vendor": "f5",
  "product": "nginx",
  "part": "a",
  "cpe": "cpe:2.3:a:f5:nginx:$1:*:*:*:*:*:*:*"
}
```

### 字段说明

| 字段名 | 类型 | 必填 | 说明 | 示例 |
|-------|------|------|------|------|
| `match_str` | string | 是 | 正则表达式，用于匹配 Banner | `"(?i)nginx/([\\d\\.]+)"` |
| `vendor` | string | 是 | 供应商名称 | `"f5"` |
| `product` | string | 是 | 产品名称 | `"nginx"` |
| `part` | string | 是 | CPE 组件类型 | `"a"` (application) |
| `cpe` | string | 否 | 目标 CPE，可含占位符 | `"cpe:2.3:a:f5:nginx:$1:*:*:*:*:*:*:*"` |

### CPE 组件类型（part）

| 值 | 说明 | 示例 |
|----|------|------|
| `a` | 应用程序 | nginx, Apache, OpenSSH |
| `o` | 操作系统 | Ubuntu, Debian, Windows |
| `h` | 硬件 | Cisco, Juniper |

### CPE 格式说明

CPE (Common Platform Enumeration) 是一种标准化的平台标识格式。

**格式**：
```
cpe:2.3:part:vendor:product:version:update:edition:language:sw_edition:target_sw:target_hw:other
```

**示例**：
```
cpe:2.3:a:f5:nginx:1.18.0:*:*:*:*:*:*:*
```

**说明**：
- `cpe:2.3` - CPE 版本
- `a` - 应用程序
- `f5` - 供应商
- `nginx` - 产品
- `1.18.0` - 版本号
- `*` - 其他字段使用通配符

## 规则文件格式

### 格式1：数组格式（向后兼容）

```json
[
  {
    "match_str": "(?i)^SSH-[\\d\\.]+-OpenSSH_([\\w\\.]+)",
    "vendor": "openbsd",
    "product": "openssh",
    "part": "a",
    "cpe": "cpe:2.3:a:openbsd:openssh:$1:*:*:*:*:*:*:*"
  },
  {
    "match_str": "(?i)nginx/([\\d\\.]+)",
    "vendor": "f5",
    "product": "nginx",
    "part": "a",
    "cpe": "cpe:2.3:a:f5:nginx:$1:*:*:*:*:*:*:*"
  }
]
```

### 格式2：标准格式（推荐）

```json
{
  "name": "Service CPE Rules",
  "version": "1.0",
  "type": "service",
  "samples": [
    {
      "match_str": "(?i)^SSH-[\\d\\.]+-OpenSSH_([\\w\\.]+)",
      "vendor": "openbsd",
      "product": "openssh",
      "part": "a",
      "cpe": "cpe:2.3:a:openbsd:openssh:$1:*:*:*:*:*:*:*"
    },
    {
      "match_str": "(?i)nginx/([\\d\\.]+)",
      "vendor": "f5",
      "product": "nginx",
      "part": "a",
      "cpe": "cpe:2.3:a:f5:nginx:$1:*:*:*:*:*:*:*"
    }
  ]
}
```

## 匹配过程详解

### 示例1：识别 Nginx 服务器

#### Banner 信息

```
HTTP/1.1 200 OK
Server: nginx/1.18.0
```

#### 规则

```json
{
  "match_str": "(?i)nginx/([\\d\\.]+)",
  "vendor": "f5",
  "product": "nginx",
  "part": "a",
  "cpe": "cpe:2.3:a:f5:nginx:$1:*:*:*:*:*:*:*"
}
```

#### 匹配过程

1. **编译正则表达式**
   ```go
   regex := regexp.MustCompile(`(?i)nginx/([\d\.]+)`)
   ```

2. **匹配 Banner**
   ```go
   submatches := regex.FindStringSubmatch("nginx/1.18.0")
   // 返回: ["nginx/1.18.0", "1.18.0"]
   ```

3. **提取版本号**
   ```go
   version := submatches[1]  // "1.18.0"
   ```

4. **填充 CPE**
   ```go
   cpe := "cpe:2.3:a:f5:nginx:$1:*:*:*:*:*:*:*"
   cpe = strings.ReplaceAll(cpe, "$1", "1.18.0")
   // 结果: "cpe:2.3:a:f5:nginx:1.18.0:*:*:*:*:*:*:*"
   ```

5. **生成匹配结果**
   ```go
   {
     Product: "nginx",
     Vendor: "f5",
     Type: "a",
     CPE: "cpe:2.3:a:f5:nginx:1.18.0:*:*:*:*:*:*:*",
     Version: "1.18.0",
     Confidence: 90,
     Source: "service_banner"
   }
   ```

### 示例2：识别 OpenSSH 服务器

#### Banner 信息

```
SSH-2.0-OpenSSH_8.2p1 Ubuntu-4ubuntu0.5
```

#### 规则

```json
{
  "match_str": "(?i)^SSH-[\\d\\.]+-OpenSSH_([\\w\\.]+)",
  "vendor": "openbsd",
  "product": "openssh",
  "part": "a",
  "cpe": "cpe:2.3:a:openbsd:openssh:$1:*:*:*:*:*:*:*"
}
```

#### 匹配过程

1. **编译正则表达式**
   ```go
   regex := regexp.MustCompile(`(?i)^SSH-[\d\.]+-OpenSSH_([\w\.]+)`)
   ```

2. **匹配 Banner**
   ```go
   submatches := regex.FindStringSubmatch("SSH-2.0-OpenSSH_8.2p1 Ubuntu-4ubuntu0.5")
   // 返回: ["SSH-2.0-OpenSSH_8.2p1", "8.2p1"]
   ```

3. **提取版本号**
   ```go
   version := submatches[1]  // "8.2p1"
   ```

4. **填充 CPE**
   ```go
   cpe := "cpe:2.3:a:openbsd:openssh:$1:*:*:*:*:*:*:*"
   cpe = strings.ReplaceAll(cpe, "$1", "8.2p1")
   // 结果: "cpe:2.3:a:openbsd:openssh:8.2p1:*:*:*:*:*:*:*"
   ```

5. **生成匹配结果**
   ```go
   {
     Product: "openssh",
     Vendor: "openbsd",
     Type: "a",
     CPE: "cpe:2.3:a:openbsd:openssh:8.2p1:*:*:*:*:*:*:*",
     Version: "8.2p1",
     Confidence: 90,
     Source: "service_banner"
   }
   ```

### 示例3：识别 Apache 服务器

#### Banner 信息

```
HTTP/1.1 200 OK
Server: Apache/2.4.41 (Ubuntu)
```

#### 规则

```json
{
  "match_str": "(?i)Apache/([\\d\\.]+)",
  "vendor": "apache",
  "product": "http_server",
  "part": "a",
  "cpe": "cpe:2.3:a:apache:http_server:$1:*:*:*:*:*:*:*"
}
```

#### 匹配过程

1. **编译正则表达式**
   ```go
   regex := regexp.MustCompile(`(?i)Apache/([\d\.]+)`)
   ```

2. **匹配 Banner**
   ```go
   submatches := regex.FindStringSubmatch("Apache/2.4.41 (Ubuntu)")
   // 返回: ["Apache/2.4.41", "2.4.41"]
   ```

3. **提取版本号**
   ```go
   version := submatches[1]  // "2.4.41"
   ```

4. **填充 CPE**
   ```go
   cpe := "cpe:2.3:a:apache:http_server:$1:*:*:*:*:*:*:*"
   cpe = strings.ReplaceAll(cpe, "$1", "2.4.41")
   // 结果: "cpe:2.3:a:apache:http_server:2.4.41:*:*:*:*:*:*:*"
   ```

5. **生成匹配结果**
   ```go
   {
     Product: "http_server",
     Vendor: "apache",
     Type: "a",
     CPE: "cpe:2.3:a:apache:http_server:2.4.41:*:*:*:*:*:*:*",
     Version: "2.4.41",
     Confidence: 90,
     Source: "service_banner"
   }
   ```

## 占位符机制

### 占位符说明

CPE 规则支持使用占位符来引用正则表达式的捕获组。

**占位符格式**：`$1`, `$2`, `$3`, ...

### 示例

#### 规则

```json
{
  "match_str": "(?i)nginx/([\\d\\.]+)\\.([\\d\\.]+)\\.([\\d\\.]+)",
  "vendor": "f5",
  "product": "nginx",
  "part": "a",
  "cpe": "cpe:2.3:a:f5:nginx:$1.$2.$3:*:*:*:*:*:*:*"
}
```

#### Banner

```
nginx/1.18.0
```

#### 匹配过程

1. **正则匹配**
   ```go
   submatches := regex.FindStringSubmatch("nginx/1.18.0")
   // 返回: ["nginx/1.18.0", "1", "18", "0"]
   ```

2. **提取捕获组**
   ```go
   $1 = "1"
   $2 = "18"
   $3 = "0"
   ```

3. **填充 CPE**
   ```go
   cpe := "cpe:2.3:a:f5:nginx:$1.$2.$3:*:*:*:*:*:*:*"
   cpe = strings.ReplaceAll(cpe, "$1", "1")
   cpe = strings.ReplaceAll(cpe, "$2", "18")
   cpe = strings.ReplaceAll(cpe, "$3", "0")
   // 结果: "cpe:2.3:a:f5:nginx:1.18.0:*:*:*:*:*:*:*"
   ```

## 正则表达式技巧

### 1. 忽略大小写

使用 `(?i)` 标志忽略大小写。

```json
{
  "match_str": "(?i)nginx/([\\d\\.]+)"
}
```

### 2. 匹配开头

使用 `^` 匹配字符串开头。

```json
{
  "match_str": "^SSH-[\\d\\.]+-OpenSSH_([\\w\\.]+)"
}
```

### 3. 匹配数字

使用 `\\d` 匹配数字。

```json
{
  "match_str": "nginx/([\\d\\.]+)"
}
```

### 4. 匹配单词字符

使用 `\\w` 匹配单词字符。

```json
{
  "match_str": "OpenSSH_([\\w\\.]+)"
}
```

### 5. 匹配点号

使用 `\\.` 匹配点号。

```json
{
  "match_str": "nginx/([\\d\\.]+)"
}
```

## 常见服务器指纹规则

### Nginx

```json
{
  "match_str": "(?i)nginx/([\\d\\.]+)",
  "vendor": "f5",
  "product": "nginx",
  "part": "a",
  "cpe": "cpe:2.3:a:f5:nginx:$1:*:*:*:*:*:*:*"
}
```

### Apache

```json
{
  "match_str": "(?i)Apache/([\\d\\.]+)",
  "vendor": "apache",
  "product": "http_server",
  "part": "a",
  "cpe": "cpe:2.3:a:apache:http_server:$1:*:*:*:*:*:*:*"
}
```

### OpenSSH

```json
{
  "match_str": "(?i)^SSH-[\\d\\.]+-OpenSSH_([\\w\\.]+)",
  "vendor": "openbsd",
  "product": "openssh",
  "part": "a",
  "cpe": "cpe:2.3:a:openbsd:openssh:$1:*:*:*:*:*:*:*"
}
```

### MySQL

```json
{
  "match_str": "(?i)MySQL/([\\d\\.]+)",
  "vendor": "oracle",
  "product": "mysql",
  "part": "a",
  "cpe": "cpe:2.3:a:oracle:mysql:$1:*:*:*:*:*:*:*"
}
```

### PostgreSQL

```json
{
  "match_str": "(?i)PostgreSQL/([\\d\\.]+)",
  "vendor": "postgresql",
  "product": "postgresql",
  "part": "a",
  "cpe": "cpe:2.3:a:postgresql:postgresql:$1:*:*:*:*:*:*:*"
}
```

### Redis

```json
{
  "match_str": "(?i)redis_version:([\\d\\.]+)",
  "vendor": "redis",
  "product": "redis",
  "part": "a",
  "cpe": "cpe:2.3:a:redis:redis:$1:*:*:*:*:*:*:*"
}
```

## 服务器指纹 vs Web 指纹

### 区别

| 特性 | 服务器指纹 | Web 指纹 |
|------|-----------|---------|
| **识别对象** | 服务器、服务、数据库 | Web 应用、CMS、框架 |
| **输入数据** | Banner 信息 | HTTP 响应（状态码、响应头、响应体） |
| **匹配方式** | 正则表达式 | 简单字段、正则表达式、JSON 规则 |
| **输出结果** | CPE、版本号 | 产品名称、厂商、CPE |
| **复杂度** | 相对简单 | 相对复杂 |

### 相似点

1. **规则加载**：都支持从数据库和 JSON 文件加载
2. **规则编译**：都会编译正则表达式
3. **匹配引擎**：都遍历规则进行匹配
4. **结果输出**：都返回标准化的匹配结果

## 总结

1. **服务器指纹识别流程**：规则加载 → Banner 获取 → 正则匹配 → 版本提取 → CPE 生成 → 结果返回
2. **规则格式**：使用正则表达式匹配 Banner，支持占位符提取版本号
3. **CPE 生成**：通过占位符替换，生成标准化的 CPE 标识
4. **匹配机制**：使用正则表达式的捕获组提取版本号等信息
5. **优势**：
   - 准确识别服务器类型和版本
   - 生成标准化的 CPE 标识
   - 支持多种服务器和服务

## 下一步

- 📖 [Web 指纹识别原理](../指纹规则匹配规则文档/01_指纹匹配原理.md) - 了解 Web 指纹的工作原理
- ✍️ [简单规则编写](../指纹规则匹配规则文档/02_简单规则编写.md) - 学习如何编写 Web 指纹规则
- 🚀 [高级规则编写](../指纹规则匹配规则文档/03_高级规则编写.md) - 学习如何编写高级 Web 指纹规则
